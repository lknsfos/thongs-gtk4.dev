APP_ID = "com.example.thongssh"

# --- КОЛОНКИ TreeStore ---
# (Имя, Тип, Иконка, Объект данных (config/node))
(
    COL_NAME,
    COL_TYPE,
    COL_ICON,
    COL_DATA
) = range(4)
# -------------------------